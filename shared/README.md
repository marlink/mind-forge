# MindForge Shared

Shared TypeScript types and utilities used across client and server.

## Usage

Import types and utilities:

```typescript
import { BaseUser, UserRole } from '@mindforge/shared';
import { formatDate, formatCurrency } from '@mindforge/shared/utils';
```

## Build

```bash
npm run build
```

This generates TypeScript declarations in the `dist/` directory.

